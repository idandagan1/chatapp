{
  "name": "client",
  "version": "1.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.22.0",
    "snyk": "^1.199.2",
    "socket.io-client": "^2.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "server": "node server.js",
    "serve": "serve --port=8080 -s build",
    "build": "react-scripts build",
    "snyk:test": "snyk test",
    "snyk-protect": "snyk protect",
    "prepare": "npm run snyk-protect"
  },
  "snyk": true
}
